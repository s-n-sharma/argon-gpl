cell if_test() {
  let x = 1.0;
  let y = float();
  let z = float();
  let w = if y < 2.0 {
    eq(z, 5.0);
    let tmp = if y < 2.0 { x } else { 2.0 };
    1.0
  } else {
    eq(z, 3.0);
    2.0
  };
  eq(x, y);

  let a = if x < 2.0 { 3.0 } else { 4.0 };
  eq(a, 5.0);
}
